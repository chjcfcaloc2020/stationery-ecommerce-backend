spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stationery_e_db
    username: root
    password: Chipheo2k3@

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect